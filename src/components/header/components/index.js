import { View, StyleSheet } from "react-native";

export const Order = () => {
  return (
    <View>
      <Image source={filter} />
      <Image source={sort} />
      <Image source={generation} />
    </View>
  );
};
